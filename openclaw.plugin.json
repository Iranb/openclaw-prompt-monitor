{
  "id": "prompt-monitor",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable the prompt monitor plugin"
      },
      "cacheDir": {
        "type": "string",
        "description": "Directory to save prompt logs (supports ~). Default: ~/.openclaw/cache/prompt-monitor"
      },
      "saveBeforeHook": {
        "type": "boolean",
        "default": true,
        "description": "Save prompt before before_agent_start hooks (Reply layer output)"
      },
      "saveAfterHook": {
        "type": "boolean",
        "default": true,
        "description": "Save final prompt actually sent to the model (includes prependContext from hooks)"
      }
    }
  }
}
