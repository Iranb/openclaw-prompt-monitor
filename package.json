{
  "name": "@openclaw/prompt-monitor",
  "version": "2026.2.4",
  "description": "OpenClaw plugin to record prompts (before hooks and final sent to model) to cache folder",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
